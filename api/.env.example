# Safe example values for the Render API service.
# Copy these names into Render Environment Variables.
JWT_SECRET=replace_with_a_long_random_secret
CORS_ORIGINS=https://replace-with-your-panel.vercel.app
